<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pastebin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1 class="text-center fw-light">Add text</h1>
    <form action="/text" method="POST" class="container mt-5r">
        <textarea name="text" rows="5" class="form-control" placeholder="Your text here" required></textarea><br>
        <button type="submit" class="btn btn-primary">Add</button>
    </form>
    <br>
    <div class="text-center">
        <h2 class="text-center rounded-pill text-bg-info">Text List</h2>
        <ul class="list-group text-center">
            <% if (list.length > 0) { %>
                <% list.forEach(text => { %>
                    <li class="list-group-item rounded-pill btn btn-primary">
                        <% if (text.content.length > 20) { %>
                            <a href="/text/<%= text.id %>" id="textlink"><%= text.content.substring(0, 20) + '...' %></a>
                        <% } else { %>
                            <%= text.content %>
                        <% } %>
                    </li>
                <% }); %>
            <% } else { %>
                <p>No text in the list yet.</p>
            <% } %>
        </ul>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>  
</body>
</html>